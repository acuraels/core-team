# 📚 Единый читательский билет для библиотеки № 14

> 🏆 Кейc хакатона **«Уральский код» — 22.11.2025 – 23.11.2025**  
> Организатор: **СБЕР** · Команда: **CoreTeam**

---

## 👥 Команда

1. **Устинов Даниил Николаевич** — тимлид, фронтенд‑разработчик  
2. **Шаматов Рафаэль Рафитович** — продуктовый менеджер, бизнес‑аналитик  
3. **Чебаненко Никита Сергеевич** — фронтенд‑разработчик  
4. **Сакевич Алексей Максимович** — ML‑инженер, дизайнер  
5. **Власенко Владислав Александрович** — бэкенд‑разработчик

---

## 📘 Описание проекта

Классическая библиотека часто воспринимается как «книгохранилище» с бумажными билетами, очередями на выдачу и отсутствием единого цифрового контура.  
**Электронный читательский билет для библиотеки № 14** решает эту проблему и превращает пространство в современный **цифровой комьюнити‑хаб**.

Ключевые идеи:

- 📱 **Единый цифровой читательский билет** — штрихкод/QR вместо пластиковой карты.  
- 🌐 **Веб‑портал библиотеки** — личные кабинеты читателя и библиотекаря, онлайн‑каталог и бронирование книг.  
- 🤖 **ИИ‑рекомендации книг** — модуль подбирает релевантную литературу на основе интересов и истории чтения.  
- 💬 **Telegram‑бот** — уведомления о бронированиях, напоминания о возврате, анонсы событий.  
- 🧭 **Интерактивная карта пространства** — наглядная навигация по залам и функциональным зонам библиотеки № 14.

---

## 🎯 Цели решения

Проект нацелен на несколько уровней влияния:

- 🧾 **Снижение операционной нагрузки** на персонал за счёт цифровизации:  
  регистрация, выдача книг, работа с бронированием, управление читателями.
- 🌟 **Популяризация библиотеки и обновление образа** — библиотека как живой городской хаб:  
  коворкинг‑зоны, дискуссионные клубы, творческие мастерские и событийная программа.
- 🔗 **Единая цифровая экосистема** для управления читателями, фондом и мероприятиями вместо разрозненных сервисов.
- 📈 **Готовность к масштабированию** — архитектура и контейнеризация позволяют развернуть решение в других библиотеках города.

---

## 🔑 Ключевые возможности

### Для читателя

- Цифровой **читательский билет** с уникальным 6‑значным кодом и штрихкодом.  
- **Личный кабинет**:
  - ФИ и логин пользователя;
  - просмотр забронированных и выданных книг;
  - избранное;
  - (в перспективе) смена пароля и настройка уведомлений.
- **Онлайн‑каталог**:
  - просмотр фонда;
  - карточки книг с обложкой, автором, годом издания, шифром хранения;
  - бронирование книги в пару кликов.
- 🤖 **ИИ‑библиотекарь** (модуль рекомендаций):
  - подбор книг на основе интересов и поведения;
  - возможность персонализированных подборок по темам.

### Для библиотекаря

- **Личный кабинет сотрудника** с ФИО, логином и служебным идентификатором.  
- Управление **читателями**:
  - список читателей и их статус по книгам;
  - отдельная карточка читателя с кодом, штрихкодом, бронированиями;
  - смена пароля читателя по запросу.
- Управление **фондом**:
  - список книг с учётом зарезервированных/выданных экземпляров;
  - просмотр активных броней по читателю;
  - действия «Выдать» и «Отклонить бронь».

### Инфраструктура и интеграции

- 🧠 **AI‑модуль** — рекомендательная система по книгам и читательским профилям (каталог `ai/`).  
- 🤖 **Telegram‑бот** — уведомления о бронированиях, сроках возврата, событиях (каталог `bot/`).  
- 🐋 **Контейнеризация** — все сервисы упакованы в Docker и поднимаются одной командой через `docker compose`.

---

## 🧩 Архитектура проекта

```text
core-team/
├─ ai/         # Модуль рекомендаций книг и профилей читателей
├─ backend/    # ASP.NET Web API (C#) — бизнес-логика, работа с БД и авторизацией
├─ bot/        # Telegram-бот уведомлений и взаимодействия с читателем
└─ frontend/   # React + TypeScript + Vite — пользовательский веб-интерфейс
```

📡 **Связь между компонентами**

```text
Читатель / Библиотекарь
          ⬇
   FRONTEND (React, Vite)
          ⬇⬆  HTTPS / REST API (JSON)
   BACKEND (ASP.NET Web API)
          ⬇
        БД (SQL)

AI-сервис рекомендаций  ← запросы от backend по профилю читателя
Telegram-бот            ← события из backend (webhook / polling)
```

---

## 🛡️ Роли и права доступа

Система использует ролевую модель, зашитую в backend:

- **Admin**
  - может создавать пользователей с ролями `Reader` и `Librarian`;
  - управляет учётными записями и конфигурацией системы.
- **Librarian**
  - имеет доступ к кабинету библиотекаря;
  - может создавать пользователей с ролью `Reader`;
  - управляет бронированиями и выдачей книг.
- **Reader**
  - имеет доступ к личному кабинету читателя;
  - просматривает каталог, бронирует и получает книги.

Роли кодируются в JWT‑токене и проверяются на backend.

---

## 🔌 Основные API‑эндпоинты

### AuthController (`/api/v1/auth`)

- `POST /login` — аутентификация пользователя, выдача `accessToken` и `refreshToken`.  
- `POST /refresh` — обновление `accessToken` по валидному `refreshToken`.

### UsersController (`/api/v1/users`)

Все эндпоинты защищены авторизацией:

- `GET /me` — данные текущего пользователя (по токену).  
- `GET /get/{id}` — получение пользователя по `id`.  
- `GET /get-all?role=Reader|Librarian|Admin` — список пользователей с фильтром по роли.  
- `POST /create` — создание нового пользователя (по правилам ролей).  
- `PUT /update/{id}` — обновление данных пользователя.  
- `DELETE /delete/{id}` — удаление пользователя.

---

## 🛠️ Технологический стек

### Frontend

- **React + TypeScript + Vite**
- CSS‑модули / кастомные стили
- Иконки — `lucide-react`
- Работа с API через fetch/axios (REST, JSON)
- Роутинги react-router-dom

### Backend

- **ASP.NET Core Web API (C#)**
- Слои:
  - контроллеры (`Api.Controllers`);
  - репозитории (`Dal.*`);
  - сервисы доменной логики (`Logic.*`).
- Аутентификация:
  - JWT‑токены (access + refresh);
  - хранение refresh‑токенов в хранилище (`Dal.Tokens`).
- Модель пользователей: `Reader`, `Librarian`, `Admin`.

### AI‑модуль (папка `ai/`)

- Сервис рекомендаций книг по профилю читателя и истории взаимодействий.
- Обмен данными с backend по HTTP/REST (планируется подключение в качестве отдельного сервиса).

### Telegram‑бот (папка `bot/`)

- Использует **Telegram Bot API** для:
  - уведомлений о бронированиях и сроках возврата;
  - рассылки анонсов событий библиотеки;
  - быстрой навигации по сервисам (ссылки на веб‑кабинет).

---

## 🚀 Запуск проекта локально

### 1. Предварительные требования

- Установлен **Docker** и **Docker Compose**  
- Свободные порты для сервисов, указанные в `docker-compose.yml`

### 2. Клонирование репозитория

```bash
git clone https://github.com/acuraels/core-team.git
cd core-team
```

### 3. Настройка переменных окружения

Минимально необходимое для демо‑запуска:

```bash
cd bot
cp .env.example .env
```

В `.env` нужно указать:

- токен Telegram‑бота (`BOT_TOKEN`);
- при необходимости — остальные параметры, описанные в `.env.example`.

Дополнительные настройки backend / ai можно задать через:

- `backend/appsettings.*.json` или переменные окружения (см. код);
- `.env`/конфиги в `ai/` (по мере реализации модуля).

### 4. Сборка и запуск всех сервисов

Из корня репозитория:

```bash
docker compose up --build
```

Docker:

- собирает контейнеры `backend`, `frontend`, `bot`, `ai` (если подключён);
- поднимает общую сеть и связывает сервисы.

После успешного старта:

- фронтенд доступен по адресу, указанному в `docker-compose.yml`  
  (например, `http://localhost:5173`),  
- backend — по REST‑эндпоинтам `http://localhost:<port>/api/v1/...`.

---

## Данные от личного кабинета (тестовые)
- Читатель: login = user123; password = user123
- Библиотекарь: login = librarian123; password = librarian123

## 🧭 Навигация по интерфейсу

- `/` — главная страница:
  - описание сервиса;
  - блок «Цифровая среда нового поколения» (вход без пластика, ИИ‑библиотекарь, события, каталог);
  - блок «Комьюнити‑хаб»;
  - интерактивная карта пространства библиотеки № 14.
- `/login` — вход в систему.  
- `/reader-profile` — личный кабинет читателя:
  - штрихкод и номер билета;
  - ФИ и логин;
  - «Моя бронь», избранное и выданные книги.
- `/librarian-profile` — личный кабинет библиотекаря:
  - данные сотрудника;
  - рабочая точка входа в сервисы учёта.
- `/books` — каталог книг (просмотр/бронь).  
- `/readers` и `/readers/:id` — список читателей и подробная карточка (для библиотекаря).

---

## 📌 Дальнейшее развитие

- Полная интеграция AI‑модуля с профилями читателей и каталогом.  
- Расширенная аналитика: популярность книг, загрузка мероприятий, вовлечённость по сегментам.  
- Переиспользуемые конфигурации для масштабирования решения на другие библиотеки города.  
- Интеграция с городскими или университетскими аккаунтами (единая учётка).

---

**CoreTeam · Уральский код 2025**  
Библиотека № 14 — больше, чем просто книги.
